@page
@model PCM_357.Pages.Admin.Matches.EditModel

@{
    ViewData["Title"] = "Sửa Trận đấu";
}

<div class="row justify-content-center py-4">
    <div class="col-lg-8">
        <div class="card shadow-lg border-0 rounded-lg">
            <div class="card-header bg-primary text-white text-center py-3 rounded-top">
                <h3 class="mb-0 fw-bold">Sửa Trận đấu</h3>
            </div>
            <div class="card-body p-4 p-md-5">
                
                <div class="alert alert-warning border-0 shadow-sm mb-4">
                    <div class="d-flex">
                        <div class="me-3">
                            <i class="bi bi-exclamation-triangle-fill fs-3 text-warning"></i>
                        </div>
                        <div>
                            <h5 class="alert-heading fw-bold">CẢNH BÁO QUAN TRỌNG:</h5>
                            <ul class="mb-0 small ps-3">
                                <li>Việc thay đổi <strong>"Đội thắng"</strong> hoặc <strong>"Ranked"</strong> tại đây sẽ <strong>KHÔNG</strong> tự động hoàn tác (revert) điểm số đã cộng trước đó.</li>
                                <li>Nếu bạn đổi người thắng, hãy cân nhắc sửa điểm Rank thủ công cho người chơi liên quan nếu cần thiết.</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="Match.Id" />

                    <!-- Date & Challenge -->
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <label asp-for="Match.Date" class="form-label fw-bold opacity-75 small text-uppercase">Ngày đấu</label>
                            <input asp-for="Match.Date" class="form-control bg-light" type="datetime-local" />
                        </div>
                        <div class="col-md-6">
                            <label asp-for="Match.ChallengeId" class="form-label fw-bold opacity-75 small text-uppercase">Sàn đấu / Sự kiện</label>
                            <select asp-for="Match.ChallengeId" class="form-select bg-light" asp-items="Model.ChallengeList">
                                <option value="">-- Giao hữu --</option>
                            </select>
                        </div>
                    </div>

                    <!-- Teams -->
                    <div class="row g-3 mb-4">
                        <!-- Team A -->
                        <div class="col-md-6">
                            <div class="card h-100 border-0 bg-light">
                                <div class="card-body">
                                    <h5 class="card-title text-primary fw-bold text-center mb-3">Team A</h5>
                                    <div class="mb-3">
                                        <label class="form-label small text-muted">Player 1</label>
                                        <select asp-for="Match.Team1_Player1Id" class="form-select" asp-items="Model.MemberList"></select>
                                    </div>
                                    <div>
                                        <label class="form-label small text-muted">Player 2</label>
                                        <select asp-for="Match.Team1_Player2Id" class="form-select" asp-items="Model.MemberList">
                                            <option value="">-- Trống --</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Team B -->
                        <div class="col-md-6">
                            <div class="card h-100 border-0 bg-light">
                                <div class="card-body">
                                    <h5 class="card-title text-danger fw-bold text-center mb-3">Team B</h5>
                                    <div class="mb-3">
                                        <label class="form-label small text-muted">Player 1</label>
                                        <select asp-for="Match.Team2_Player1Id" class="form-select" asp-items="Model.MemberList"></select>
                                    </div>
                                    <div>
                                        <label class="form-label small text-muted">Player 2</label>
                                        <select asp-for="Match.Team2_Player2Id" class="form-select" asp-items="Model.MemberList">
                                            <option value="">-- Trống --</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Result & Ranked -->
                    <div class="p-3 bg-light rounded-3 mb-4 border border-light">
                        <div class="mb-3">
                             <label asp-for="Match.WinningSide" class="form-label fw-bold text-uppercase">Kết Quả Trận Đấu</label>
                             <select asp-for="Match.WinningSide" class="form-select form-select-lg">
                                <option value="None">Chưa xác định</option>
                                <option value="Team1">Team 1 Thắng</option>
                                <option value="Team2">Team 2 Thắng</option>
                            </select>
                        </div>
                        <div class="form-check form-switch custom-switch">
                             <input class="form-check-input" asp-for="Match.IsRanked" id="rankedSwitch" />
                             <label class="form-check-label fw-bold" for="rankedSwitch">Ranked Game (Tính điểm)</label>
                        </div>
                    </div>

                    <!-- Actions -->
                    <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                        <a asp-page="./Index" class="btn btn-outline-secondary px-4">
                            <i class="bi bi-arrow-left"></i> Quay lại
                        </a>
                        <button type="submit" class="btn btn-danger btn-lg px-4 shadow-sm">
                            <i class="bi bi-save"></i> Lưu Thay Đổi (Cẩn trọng)
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
